/* 
  Accept  ASCII code and display its value
*/

#include <stdio.h>

int main ()
{
 char ch;
 printf("/n enter the ASCII:");
 scanf ("%c",&ch );
 printf("ASCII value for %c is %d.", ch,ch);
 return 0;
}

